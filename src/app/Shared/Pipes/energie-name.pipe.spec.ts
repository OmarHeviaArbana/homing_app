import { EnergieNamePipe } from './energie-name.pipe';

describe('EnergieNamePipe', () => {
  it('create an instance', () => {
    const pipe = new EnergieNamePipe();
    expect(pipe).toBeTruthy();
  });
});
